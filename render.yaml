services:
  - type: web
    name: lol-server
    env: node
    root: server
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
    plan: free
